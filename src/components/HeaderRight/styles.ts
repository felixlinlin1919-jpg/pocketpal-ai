import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  headerRightContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 2,
  },
  newChatBtn: {
    marginLeft: 0,
    marginRight: 0,
  },
  menuBtn: {
    marginLeft: 0,
    marginRight: 0,
  },
});
